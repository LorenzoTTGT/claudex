---
name: clickup-sync
description: "Fetch tasks from a ClickUp list and refresh the local task tracker when explicitly asked to sync remote ClickUp data."
---

# ClickUp Sync

When explicitly asked to sync a ClickUp list, fetch all pages, fetch per-task details and attachments when needed, group results by status/category, and update the local tracker while preserving local annotations where appropriate.

Report the sync timestamp and any API errors clearly.
